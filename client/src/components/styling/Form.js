import styled from 'styled-components';

const Form = styled.form`
    max-width: 300px;
    background: #FAFAFA;
    padding: 30px;
    margin: 50px auto;
    box-shadow: 1px 1px 25px rgba(0, 0, 0, 0.35);
    border-radius: 10px;
    border: 6px solid #006655;
`;

export default Form;